name: 15 script add to workflow

on:
    push:

jobs:
    run-script:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Execute build script
              shell: bash
              run: ./../scripts/build.sh